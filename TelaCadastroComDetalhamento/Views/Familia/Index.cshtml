@model IEnumerable<TelaCadastroComDetalhamento.Models.Pai>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayName("Pai")
            </th>
            <th>
                @Html.DisplayName("Filho")
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var pai in Model) {
    foreach(var filho in pai.Filhos)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => pai.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => filho.Nome)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
    }
}
    </tbody>
</table>
